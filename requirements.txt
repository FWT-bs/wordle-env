# Optional environment-only dependencies go here.
#
# This example only uses the Python standard library for task logic.
# For local development, install the Mesocosm CLI/runtime separately:
#   pip install swecc-mesocosm
